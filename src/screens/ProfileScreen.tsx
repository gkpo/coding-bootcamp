import { Placeholder } from './Placeholder';

export function ProfileScreen() {
  return (
    <Placeholder
      title="Me"
      lede="Streak calendar, weekly XP, per-track mastery, settings."
      milestone="M5"
    />
  );
}
